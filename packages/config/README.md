# Shared Config

Shared TypeScript configuration for the Menu Sync System monorepo.

## Files

- `tsconfig.base.json`: shared strict TypeScript defaults
- `tsconfig.astro.json`: admin UI TypeScript defaults
- `tsconfig.worker.json`: Cloudflare Workers TypeScript defaults

No runtime implementation lives in this package.
